name: Deploy to EC2

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Gradle
        run: ./gradlew build -x test

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      - name: Deploy to S3
        run: |
          aws s3 cp build/libs/*.jar s3://cicd-s3-payment/payment-api-0.0.1-SNAPSHOT.jar
          aws s3 cp appspec.yml s3://cicd-s3-payment/appspec.yml
          aws s3 cp --recursive scripts s3://cicd-s3-payment/scripts/

      - name: Deploy to EC2 with CodeDeploy
        run: |
          aws deploy create-deployment \
            --application-name codedepoly \
            --deployment-group-name production-deploy \
            --s3-location bucket=cicd-s3-payment,bundleType=zip,key=app.zip